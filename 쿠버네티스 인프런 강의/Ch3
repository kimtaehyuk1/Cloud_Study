- 디플로이먼트를 노출하려면?
1. 디플로이먼트를 노드포트로 노출하기

kubectl expose deployment deploy-nginx --type=NodePort --port=80
(디플로이먼트를 노드포트로 노출하기)

kubectl get services
(서비스로 노출됫는지 확인하기 여기서 포트번호도 확인가능)

근데 노드포트로 노출하려면 일단 노드의
